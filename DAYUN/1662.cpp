#include <iostream>

using namespace std;

int main()
{
    cin.tie(0);
    cout.tie(0);
    ios::sync_with_stdio(false);

    //압축되지 않은 문자열 s k(q)와 같이 압축
    //k는 한자리  정수q는 0자리 이상의 문자열
    string str;
    cin >> str;

    /*
    9-> 1 
    79*2 158
    3 56158 * 3
    */

   //1. (마지막 문자열에서 ) 뺴고 숫자 찾기 
   // 첫번째 숫자 찾기 ( 숫자 ) 인것
   //while문 돌면서 ( ... 하고 다시 ( 발견 못하고 그다음 )발생
   //(숫자) 계산 vector에서 () 제거하고 vector에서 (char)숫자 추가하기
   //반복 () 안나올때 까지 
   //length 계산





    //반복되는 모든 문자열을 저장



	return 0;
}